# Decision 0010 amendment 04: comprehensive React continuous execution

Status: Accepted upon materialization under Andrew / `ndrewtran`'s prior
digest-specific acceptance of the exact R1 continuous-execution candidate and
canonical materialization manifest. The authority PR retains those identities
and the deterministically rendered acceptance record.

Decision: `core-ui:decision:0010:amendment:04`

Parent decision: `core-ui:decision:0010`

## Unchanged product decision

The immutable React Aria Components `1.20.0` snapshot, exactly 53 committed
Core-owned React families, 45 new plus eight reused Scope IDs, R1.1/R1.2/R1.3/
R1.4 tranche counts `11/11/24/7`, R1.5 closure-only role, Mux UI styling,
Core public ownership, React `0.1` package boundary, and later-track exclusions
from amendment 03 remain exact.

React Aria remains an internal replaceable substrate. Mux UI styling remains
Core-owned rather than a dependency or live owner. RSC/client-boundary,
framework-free web, React Native, React Native Web, cross-renderer equivalence,
stable, `latest`, public catalog/tooling, Scale, and production work remain
outside R1.

The complete accepted execution envelope, canonical materialization manifest,
and deterministically rendered acceptance record are repository authority at:

- `decisions/0010-amendment-04-r1-continuous-execution-envelope.md`;
- `decisions/0010-amendment-04-r1-continuous-execution-materialization.json`;
  and
- `decisions/0010-amendment-04-r1-continuous-execution-acceptance.md`.

Every conditional lock, evidence, Project, Git, or intermediate-merge claim
must resolve and verify those exact records through the private conformance
owner named by Decision 0009 amendment 03. Missing or mismatched authority or
acceptance fails closed. Only the bounded verifier-bootstrap PR may precede
that verifier.

## Replaced amendment-03 boundaries

For work performed under the accepted R1 continuous-execution envelope only:

1. Amendment 03 lines 195-199 are amended so activation requires an exact
   mechanically derived tranche lock admitted by the continuous-execution
   envelope. Its post-proof human evidence message is replaced by conditional
   evidence acceptance after every deterministic, negative-path,
   risk-selected manual/AT, disclosure, immutable-relation, and
   profile-selected independent-review predicate passes.
2. Lines 241-247 are amended so a lock proceeds after deterministic conformity
   to the envelope. Focused proof, risk-selected independent review,
   applicable manual browser/AT proof, packed-consumer validation, and failure
   evidence remain mandatory.
3. Lines 256-260 remain unchanged. Every registry mutation still requires its
   own exact publication authorization and final collision/drift check.
4. Lines 299-304 are amended only so the candidate's standing human decision
   authorizes the exact fail-closed Project migration and in-envelope
   reconciliation; no second Project approval is required.
5. Lines 308-325 remain historical statements about amendment 03's original
   authorization. This amendment separately authorizes the bounded repository,
   Git, Project, implementation, evidence, and intermediate-merge operations;
   it does not authorize npm publication or the final R1-exit PR merge.

No other Decision 0010 amendment-03 meaning is changed.

## Delegated initial Core contract

For a committed family whose initial Core `0.1` contract is not authored,
Andrew delegates the bounded initial-contract decision to the Core UI delivery
lead. Before implementation, the tranche lock freezes the Core-owned family
name, exports, props, types, defaults, slots, events, state, composition, DOM,
accessibility obligations, and styling hooks.

The contract must pass independent ownership, ergonomics, compatibility,
accessibility, Mux UI styling, and React-Aria-internal-only review. It may not expose
a raw React Aria API by default, import another library's ownership model, add a
family, add a runtime graph node, or claim stable/support behavior. A contract
outside the committed family or a later breaking revision remains
decision-bearing and returns to Andrew.

## Lock and evidence admission

An R1.1-R1.5 lock needs no later human message only when it derives exactly
from the accepted 53-family snapshot, fixed tranche registry, Product Scope
6.0.1, accepted current R1.0 baseline, canonical owner revisions, and unchanged
milestone evidence/non-goal contract.

Retained evidence needs no later human message only when every exact required
result and relation passes. The standing decision does not claim Andrew
inspected future bytes. A missing or inconclusive predicate leaves the evidence
unaccepted and the family/tranche incomplete.

## R1.4 correction

Every component named by the exact R1.4 lock must be export-ready and have
complete evidence for its exact contract and risk profile. Missing proof keeps
the component unexported and blocks R1.4. An alpha candidate may be prepared
only after all seven R1.4 families satisfy that exit. No publication, secondary
renderer, stable, `latest`, or equivalence claim follows.

## Reversal

If this amendment is absent, unaccepted, mismatched, or superseded, amendment
03's original separate-acceptance boundaries remain in force. Reversal is
append-only and cannot rewrite completed evidence, Project history, or releases.
