<!-- Clonk-Mode: feature -->
# Goal — You are the sole implementation worker for muxui. Continue from the existing iso

## Goal
You are the sole implementation worker for muxui. Continue from the existing isolated target /tmp/muxui-overlay-motion-preview. The project goal is to make the best app ever. Finish the static motion preview with: A exact BeUI values, B -4/.985 travel, C stiffness420 softer spring, plus dark/light modes and CommandPalette, Dialog, and AlertDialog states. Correct the harmless malformed apply_patch attempt, bundle the preview, serve it on a fresh localhost port, then polish and QA it using concrete browser/local checks. The tracked repository at /Users/admin/Projects/muxui has unrelated in-progress changes: do not edit tracked repository files, do not commit, push, open a PR, or touch production. Keep all work isolated to /tmp/muxui-overlay-motion-preview. When complete, report the exact preview URL, what was verified, and any remaining caveat. Work autonomously.

## Scope
This is ONE feature inside an already-existing app. Build only what the goal
asks for. Reuse the app's existing patterns, components, and conventions. Do
NOT add onboarding, paywalls, or unrelated scaffolding unless the goal asks.

## Done When
<!-- Clonk defines observable acceptance criteria during planning -->
